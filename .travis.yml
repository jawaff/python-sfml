matrix:
  include:
  - os: linux
    language: python
    python: "2.7"
    # The '.' executes this script within the current shell.
    before_install: chmod +x scripts/sfml_install.sh && . scripts/sfml_install.sh
  - os: linux
    language: python
    python: "3.7-dev"
    # The '.' executes this script within the current shell.
    before_install: chmod +x scripts/sfml_install.sh && . scripts/sfml_install.sh
    
    
notifications:
  email: false

install:
  - pip install -r requirements.txt
  # --user bypasses the need for the sudo level of authority.
  - python setup.py sdist bdist_wheel
#   # Handles the push to pypi.
#   - twine upload --repository-url https://upload.pypi.org/legacy/ dist/*

script:
-  echo "Warning: We current have no success check in the .travis.yml file."
#   TODO: Explore integration testing.
#   - pytest cython_example_proj
  
# deploy:
#   provider: pypi
#   user: jawaff
#   password:
#     secure: <SOME_KEY>
#   on:
#     branch: master
